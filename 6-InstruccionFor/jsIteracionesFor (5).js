function mostrar()
{

	var numeroIngresado;

	numeroIngresado = 0;

	for ( i = 0; i >= 0; i++) {
	
		numeroIngresado=prompt("Ingrese un número y presione aceptar." +
	'\n' + "Ingrese 9 para terminar:")

		if(numeroIngresado == 9){ break; }
	
	}
}//FIN DE LA FUNCIÓN